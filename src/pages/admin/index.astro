---
import AdminLayout from '../../layouts/AdminLayout.astro';
---
<AdminLayout title="Appointments" activePage="dashboard">

  <!-- Page header -->
  <div class="flex flex-col md:flex-row items-start md:items-center justify-between mb-8 gap-4">
    <div>
      <h1 class="font-['DM_Sans'] font-bold text-[1.375rem] text-slate-900 m-0">Appointments</h1>
      <p class="text-[0.8125rem] text-slate-400 mt-1">Manage and update patient appointment status</p>
    </div>
    <div class="flex gap-3 items-center">
      <select id="status-filter" class="border border-slate-200 rounded-lg py-2 px-3 text-[0.8125rem] text-slate-700 bg-white focus:outline-none focus:ring-2 focus:ring-sky-500 cursor-pointer">
        <option value="all">All statuses</option>
        <option value="pending">Pending</option>
        <option value="confirmed">Confirmed</option>
        <option value="cancelled">Cancelled</option>
        <option value="completed">Completed</option>
      </select>
    </div>
  </div>

  <!-- Stats row -->
  <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-8">
    <div class="bg-white border border-slate-200 border-t-[3px] border-t-indigo-500 rounded-xl p-5 shadow-sm relative overflow-hidden">
      <div class="absolute -right-2 -bottom-2 opacity-5 text-indigo-500 pointer-events-none">
        <svg class="w-20 h-20" fill="currentColor" viewBox="0 0 20 20"><path d="M9 2a1 1 0 000 2h2a1 1 0 100-2H9z" /><path fill-rule="evenodd" d="M4 5a2 2 0 012-2 3 3 0 003 3h2a3 3 0 003-3 2 2 0 012 2v11a2 2 0 01-2 2H6a2 2 0 01-2-2V5zm3 4a1 1 0 000 2h.01a1 1 0 100-2H7zm3 0a1 1 0 000 2h3a1 1 0 100-2h-3zm-3 4a1 1 0 100 2h.01a1 1 0 100-2H7zm3 0a1 1 0 100 2h3a1 1 0 100-2h-3z" clip-rule="evenodd" /></svg>
      </div>
      <p id="stat-total" class="font-['DM_Sans'] font-extrabold text-3xl text-slate-900 leading-none m-0 relative z-10">—</p>
      <p class="text-xs text-slate-400 mt-2 uppercase tracking-wider font-medium relative z-10">Total</p>
    </div>
    <div class="bg-white border border-slate-200 border-t-[3px] border-t-amber-500 rounded-xl p-5 shadow-sm relative overflow-hidden">
      <div class="absolute -right-2 -bottom-2 opacity-5 text-amber-500 pointer-events-none">
        <svg class="w-20 h-20" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z" clip-rule="evenodd" /></svg>
      </div>
      <p id="stat-pending" class="font-['DM_Sans'] font-extrabold text-3xl text-slate-900 leading-none m-0 relative z-10">—</p>
      <p class="text-xs text-slate-400 mt-2 uppercase tracking-wider font-medium relative z-10">Pending</p>
    </div>
    <div class="bg-white border border-slate-200 border-t-[3px] border-t-emerald-500 rounded-xl p-5 shadow-sm relative overflow-hidden">
      <div class="absolute -right-2 -bottom-2 opacity-5 text-emerald-500 pointer-events-none">
        <svg class="w-20 h-20" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M6.267 3.455a3.066 3.066 0 001.745-.723 3.066 3.066 0 013.976 0 3.066 3.066 0 001.745.723 3.066 3.066 0 012.812 2.812c.051.643.304 1.254.723 1.745a3.066 3.066 0 010 3.976 3.066 3.066 0 00-.723 1.745 3.066 3.066 0 01-2.812 2.812 3.066 3.066 0 00-1.745.723 3.066 3.066 0 01-3.976 0 3.066 3.066 0 00-1.745-.723 3.066 3.066 0 01-2.812-2.812 3.066 3.066 0 00-.723-1.745 3.066 3.066 0 010-3.976 3.066 3.066 0 00.723-1.745 3.066 3.066 0 012.812-2.812zm7.44 5.252a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" /></svg>
      </div>
      <p id="stat-confirmed" class="font-['DM_Sans'] font-extrabold text-3xl text-slate-900 leading-none m-0 relative z-10">—</p>
      <p class="text-xs text-slate-400 mt-2 uppercase tracking-wider font-medium relative z-10">Confirmed</p>
    </div>
    <div class="bg-white border border-slate-200 border-t-[3px] border-t-sky-500 rounded-xl p-5 shadow-sm relative overflow-hidden">
      <div class="absolute -right-2 -bottom-2 opacity-5 text-sky-500 pointer-events-none">
        <svg class="w-20 h-20" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M6 2a1 1 0 00-1 1v1H4a2 2 0 00-2 2v10a2 2 0 002 2h12a2 2 0 002-2V6a2 2 0 00-2-2h-1V3a1 1 0 10-2 0v1H7V3a1 1 0 00-1-1zm0 5a1 1 0 000 2h8a1 1 0 100-2H6z" clip-rule="evenodd" /></svg>
      </div>
      <p id="stat-today" class="font-['DM_Sans'] font-extrabold text-3xl text-slate-900 leading-none m-0 relative z-10">—</p>
      <p class="text-xs text-slate-400 mt-2 uppercase tracking-wider font-medium relative z-10">Today</p>
    </div>
  </div>

  <!-- Filter Bar -->
  <div class="flex flex-col md:flex-row gap-4 items-stretch md:items-center mb-6">
    <div class="relative flex-1">
      <input type="text" id="search-input" placeholder="Search by patient name..." 
        class="w-full border border-slate-200 rounded-lg py-2 pl-9 pr-3 text-[0.8125rem] text-slate-700 bg-white focus:outline-none focus:ring-2 focus:ring-sky-500" />
      <span class="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400 text-sm">⌕</span>
    </div>
    
    <div class="flex gap-2 overflow-x-auto pb-1 md:pb-0 hide-scroll">
      <button class="date-filter-btn shrink-0 bg-sky-500 text-white border-sky-500 border rounded-lg py-1.5 px-3 text-xs font-medium transition-colors" data-filter="all">All</button>
      <button class="date-filter-btn shrink-0 bg-white text-slate-500 border-slate-200 border rounded-lg py-1.5 px-3 text-xs font-medium transition-colors hover:bg-slate-50" data-filter="today">Today</button>
      <button class="date-filter-btn shrink-0 bg-white text-slate-500 border-slate-200 border rounded-lg py-1.5 px-3 text-xs font-medium transition-colors hover:bg-slate-50" data-filter="week">This Week</button>
      <button class="date-filter-btn shrink-0 bg-white text-slate-500 border-slate-200 border rounded-lg py-1.5 px-3 text-xs font-medium transition-colors hover:bg-slate-50" data-filter="month">This Month</button>
    </div>
  </div>

  <!-- Delete Error Banner -->
  <div id="delete-error" class="hidden mb-6 p-4 rounded-lg bg-red-50 border border-red-100 text-sm items-start gap-3 transition-opacity">
    <span class="text-red-500 text-lg leading-none mt-0.5">⚠</span>
    <div>
      <p class="font-medium text-red-800">Failed to delete appointment</p>
      <p id="delete-error-msg" class="text-red-600 mt-0.5"></p>
    </div>
  </div>

  <!-- Table & Cards Container -->
  <div class="bg-white border border-slate-200 rounded-xl shadow-sm overflow-hidden">
    <div class="py-4 px-6 border-b border-slate-100 flex items-center justify-between">
      <p class="text-[0.8125rem] font-semibold text-slate-900">All Appointments</p>
      <p id="row-count" class="text-xs text-slate-400 font-mono"></p>
    </div>
    
    <!-- Desktop Table -->
    <div class="hidden md:block overflow-x-auto">
      <table class="w-full border-collapse text-[0.8125rem]">
        <thead>
          <tr class="border-b border-slate-100 bg-slate-50/50">
            <th class="text-left py-3 px-6 text-[0.6875rem] font-semibold text-slate-400 uppercase tracking-wider whitespace-nowrap">Patient</th>
            <th class="text-left py-3 px-4 text-[0.6875rem] font-semibold text-slate-400 uppercase tracking-wider whitespace-nowrap">Date</th>
            <th class="text-left py-3 px-4 text-[0.6875rem] font-semibold text-slate-400 uppercase tracking-wider whitespace-nowrap">Time</th>
            <th class="text-left py-3 px-4 text-[0.6875rem] font-semibold text-slate-400 uppercase tracking-wider whitespace-nowrap">Treatment</th>
            <th class="text-left py-3 px-4 text-[0.6875rem] font-semibold text-slate-400 uppercase tracking-wider whitespace-nowrap">Status</th>
            <th class="text-left py-3 px-6 text-[0.6875rem] font-semibold text-slate-400 uppercase tracking-wider whitespace-nowrap text-right">Actions</th>
          </tr>
        </thead>
        <tbody id="appointments-tbody">
          <tr><td colspan="6" class="p-12"><div class="flex justify-center"><div class="w-6 h-6 border-2 border-sky-200 border-t-sky-500 rounded-full animate-spin"></div></div></td></tr>
        </tbody>
      </table>
    </div>
    
    <!-- Mobile Cards -->
    <div id="appointments-mobile" class="md:hidden flex flex-col divide-y divide-slate-100">
      <div class="p-12 flex justify-center"><div class="w-6 h-6 border-2 border-sky-200 border-t-sky-500 rounded-full animate-spin"></div></div>
    </div>
  </div>

  <!-- Delete Confirmation Modal -->
  <div id="delete-modal" class="fixed inset-0 z-50 hidden items-center justify-center">
    <div class="absolute inset-0 bg-slate-900/50 backdrop-blur-sm" onclick="closeDeleteModal()"></div>
    <div class="bg-white rounded-xl shadow-xl w-full max-w-md mx-4 relative z-10 overflow-hidden animate-in fade-in zoom-in-95 duration-200">
      <div class="p-6">
        <div class="flex items-center gap-4 mb-4">
          <div class="w-10 h-10 rounded-full bg-red-100 flex items-center justify-center shrink-0">
            <span class="text-red-600 text-xl font-bold">!</span>
          </div>
          <h3 class="font-['DM_Sans'] font-bold text-xl text-slate-900 m-0">Delete appointment?</h3>
        </div>
        <p class="text-[0.9375rem] text-slate-500 leading-relaxed">
          This will permanently delete this appointment. This cannot be undone.
        </p>
      </div>
      <div class="bg-slate-50 px-6 py-4 flex items-center justify-end gap-3 border-t border-slate-100">
        <button onclick="closeDeleteModal()" class="px-4 py-2 text-sm font-medium text-slate-600 bg-transparent hover:bg-slate-200 rounded-lg transition-colors">
          Go back
        </button>
        <button onclick="executeDelete()" class="px-4 py-2 text-sm font-medium text-white bg-red-500 hover:bg-red-600 rounded-lg transition-colors shadow-sm">
          Yes, delete appointment
        </button>
      </div>
    </div>
  </div>

</AdminLayout>

<style>
  .hide-scroll::-webkit-scrollbar {
    display: none;
  }
  .hide-scroll {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }
</style>

<script>
  import { supabase } from '../../lib/supabase';
  import { escapeHTML } from '../../lib/utils';

  let allAppointments: any[] = [];
  let currentSearchTerm = '';
  let currentDateFilter = 'all';

  async function loadAppointments(statusFilter = 'all') {
    let query = supabase
      .from('appointments')
      .select('*, patients(full_name, phone)')
      .order('appointment_date', { ascending: true });

    if (statusFilter !== 'all') query = query.eq('status', statusFilter);

    const { data, error } = await query;
    if (error) { 
      console.error(error); 
      const tbody = document.getElementById('appointments-tbody');
      if (tbody) {
        tbody.innerHTML = '<tr><td colspan="6" class="p-12 text-center text-red-500 text-[0.8125rem]">Failed to load appointments. Please refresh.</td></tr>';
      }
      return; 
    }

    allAppointments = data || [];
    renderTable();
  }

  function renderTable() {
    const todayStr = new Date().toISOString().split('T')[0];
    const today = new Date(todayStr);

    const filtered = allAppointments.filter(a => {
      // 1. Search Filter
      const nameMatch = a.patients?.full_name?.toLowerCase().includes(currentSearchTerm.toLowerCase());
      if (currentSearchTerm && !nameMatch) return false;

      // 2. Date Filter
      if (currentDateFilter === 'today') {
        return a.appointment_date === todayStr;
      } else if (currentDateFilter === 'week') {
        const appDate = new Date(a.appointment_date);
        const dayOfWeek = today.getDay(); // 0 is Sunday
        const startOfWeek = new Date(today);
        startOfWeek.setDate(today.getDate() - dayOfWeek);
        const endOfWeek = new Date(today);
        endOfWeek.setDate(today.getDate() + (6 - dayOfWeek));
        return appDate >= startOfWeek && appDate <= endOfWeek;
      } else if (currentDateFilter === 'month') {
        return a.appointment_date.startsWith(todayStr.slice(0, 7)); // e.g. "2023-10"
      }
      return true;
    });

    const totalEl = document.getElementById('stat-total');
    const pendingEl = document.getElementById('stat-pending');
    const confirmedEl = document.getElementById('stat-confirmed');
    const todayEl = document.getElementById('stat-today');
    const countEl = document.getElementById('row-count');

    if (totalEl) totalEl.textContent = String(filtered.length);
    if (pendingEl) pendingEl.textContent = String(filtered.filter((a: any) => a.status === 'pending').length);
    if (confirmedEl) confirmedEl.textContent = String(filtered.filter((a: any) => a.status === 'confirmed').length);
    if (todayEl) todayEl.textContent = String(filtered.filter((a: any) => a.appointment_date === todayStr).length);
    if (countEl) countEl.textContent = `${filtered.length} records`;

    const tbody = document.getElementById('appointments-tbody')!;
    const mobileContainer = document.getElementById('appointments-mobile')!;
    
    if (filtered.length === 0) {
      const emptyHtml = `
        <div class="p-12 text-center flex flex-col items-center justify-center">
          <svg class="w-12 h-12 text-slate-300 mb-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
          </svg>
          <p class="font-medium text-slate-900 text-[0.9375rem] mb-1">No appointments found</p>
          <p class="text-slate-500 text-[0.8125rem]">Try adjusting your search or date filters.</p>
        </div>`;
      tbody.innerHTML = `<tr><td colspan="6">${emptyHtml}</td></tr>`;
      mobileContainer.innerHTML = emptyHtml;
      return;
    }

    const statusStyles: Record<string, string> = {
      pending:   'bg-amber-100 text-amber-800',
      confirmed: 'bg-emerald-100 text-emerald-800',
      cancelled: 'bg-red-100 text-red-800',
      completed: 'bg-slate-100 text-slate-600',
    };

    const rowColors: Record<string, string> = {
      pending:   'bg-amber-50/20 hover:bg-amber-50/60',
      confirmed: 'bg-emerald-50/20 hover:bg-emerald-50/60',
      cancelled: 'bg-red-50/20 hover:bg-red-50/60',
      completed: 'bg-slate-50/30 hover:bg-slate-100/50',
    };

    const generateActions = (a: any, prefix: string) => {
      let menuItems = '';
      if (a.status === 'pending') {
        menuItems += `<button onclick="updateStatus('${escapeHTML(a.id)}','confirmed')" class="w-full text-left px-4 py-2 text-[0.8125rem] font-medium text-emerald-700 hover:bg-emerald-50 transition-colors">Confirm</button>`;
        menuItems += `<button onclick="updateStatus('${escapeHTML(a.id)}','cancelled')" class="w-full text-left px-4 py-2 text-[0.8125rem] font-medium text-red-700 hover:bg-red-50 transition-colors">Cancel</button>`;
      } else if (a.status === 'confirmed') {
        menuItems += `<button onclick="updateStatus('${escapeHTML(a.id)}','completed')" class="w-full text-left px-4 py-2 text-[0.8125rem] font-medium text-slate-700 hover:bg-slate-100 transition-colors">Mark as Done</button>`;
        menuItems += `<button onclick="updateStatus('${escapeHTML(a.id)}','cancelled')" class="w-full text-left px-4 py-2 text-[0.8125rem] font-medium text-red-700 hover:bg-red-50 transition-colors">Cancel</button>`;
      }
      menuItems += `<button onclick="confirmDeleteAppointment('${escapeHTML(a.id)}')" class="w-full text-left px-4 py-2 text-[0.8125rem] font-medium text-red-600 hover:bg-red-50 transition-colors">Delete</button>`;

      return `
        <div class="relative dropdown-container inline-block text-left">
          <button onclick="toggleDropdown(event, 'action-${prefix}${escapeHTML(a.id)}')" class="p-1.5 text-slate-400 hover:text-slate-600 hover:bg-slate-100 rounded-md transition-colors btn-press focus:outline-none focus:ring-2 focus:ring-sky-500">
            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20"><path d="M10 6a2 2 0 110-4 2 2 0 010 4zM10 12a2 2 0 110-4 2 2 0 010 4zM10 18a2 2 0 110-4 2 2 0 010 4z"/></svg>
          </button>
          <div id="action-${prefix}${escapeHTML(a.id)}" class="action-dropdown hidden absolute right-0 top-full mt-1 w-36 bg-white rounded-lg shadow-lg border border-slate-100 overflow-hidden z-20 py-1">
            ${menuItems}
          </div>
        </div>
      `;
    };

    // Desktop
    tbody.innerHTML = filtered.map((a: any) => {
      const bg = rowColors[a.status] || 'bg-white hover:bg-slate-50';
      return `
      <tr class="border-b border-slate-50 ${bg} transition-colors">
        <td class="py-3 px-6">
          <p class="font-medium text-slate-900 text-[0.8125rem] whitespace-nowrap">${escapeHTML(a.patients?.full_name || '—')}</p>
          <p class="text-[0.6875rem] text-slate-400 mt-0.5 whitespace-nowrap">${escapeHTML(a.patients?.phone || '')}</p>
        </td>
        <td class="py-3 px-4 font-mono text-slate-600 text-xs whitespace-nowrap">${escapeHTML(a.appointment_date)}</td>
        <td class="py-3 px-4 font-mono text-slate-600 text-xs whitespace-nowrap">${escapeHTML(a.appointment_time?.slice(0,5))}</td>
        <td class="py-3 px-4 text-slate-700 text-[0.8125rem] whitespace-nowrap">${escapeHTML(a.treatment_type)}</td>
        <td class="py-3 px-4 whitespace-nowrap">
          <span class="${escapeHTML(statusStyles[a.status] || '')} rounded-md py-1 px-2.5 text-[0.6875rem] font-semibold capitalize inline-block">${escapeHTML(a.status)}</span>
        </td>
        <td class="py-3 px-6 whitespace-nowrap text-right">
          ${generateActions(a, 'desktop-')}
        </td>
      </tr>
      `;
    }).join('');

    // Mobile
    mobileContainer.innerHTML = filtered.map((a: any) => {
      const bg = rowColors[a.status] || 'bg-white';
      return `
      <div class="${bg} p-4 transition-colors">
        <div class="flex justify-between items-start mb-2">
          <div>
            <p class="font-medium text-slate-900 text-[0.9375rem]">${escapeHTML(a.patients?.full_name || '—')}</p>
            <p class="text-xs text-slate-400 mt-0.5">${escapeHTML(a.patients?.phone || '')}</p>
          </div>
          ${generateActions(a, 'mobile-')}
        </div>
        <div class="grid grid-cols-2 gap-y-2 mt-3">
          <div>
            <p class="text-[0.625rem] text-slate-400 uppercase tracking-wider font-semibold">Date & Time</p>
            <p class="font-mono text-[0.8125rem] text-slate-700">${escapeHTML(a.appointment_date)} <span class="text-slate-400 mx-1">•</span> ${escapeHTML(a.appointment_time?.slice(0,5))}</p>
          </div>
          <div>
            <p class="text-[0.625rem] text-slate-400 uppercase tracking-wider font-semibold">Status</p>
            <span class="${escapeHTML(statusStyles[a.status] || '')} rounded border-none py-0.5 px-2 text-[0.6875rem] font-semibold capitalize inline-block mt-0.5">${escapeHTML(a.status)}</span>
          </div>
          <div class="col-span-2">
            <p class="text-[0.625rem] text-slate-400 uppercase tracking-wider font-semibold">Treatment</p>
            <p class="text-[0.8125rem] text-slate-700">${escapeHTML(a.treatment_type)}</p>
          </div>
        </div>
      </div>
      `;
    }).join('');
  }

  (window as any).toggleDropdown = (event: Event, id: string) => {
    const el = document.getElementById(id);
    const wasHidden = el?.classList.contains('hidden');
    // Hide all others
    document.querySelectorAll('.action-dropdown').forEach(d => d.classList.add('hidden'));
    
    const overlay = document.getElementById('dropdown-overlay');
    // Toggle targeted one
    if (wasHidden) {
      el?.classList.remove('hidden');
      if (overlay) overlay.classList.remove('hidden');
    } else {
      if (overlay) overlay.classList.add('hidden');
    }
  };

  (window as any).updateStatus = async (id: string, status: string) => {
    await supabase.from('appointments').update({ status }).eq('id', id);
    loadAppointments((document.getElementById('status-filter') as HTMLSelectElement).value);
  };

  document.getElementById('status-filter')?.addEventListener('change', (e) => {
    loadAppointments((e.target as HTMLSelectElement).value);
  });

  document.getElementById('search-input')?.addEventListener('input', (e) => {
    currentSearchTerm = (e.target as HTMLInputElement).value;
    renderTable();
  });

  document.querySelectorAll('.date-filter-btn').forEach(btn => {
    btn.addEventListener('click', (e) => {
      const target = e.target as HTMLButtonElement;
      currentDateFilter = target.dataset.filter || 'all';
      
      document.querySelectorAll('.date-filter-btn').forEach(b => {
        b.className = 'date-filter-btn shrink-0 bg-white text-slate-500 border-slate-200 border rounded-lg py-1.5 px-3 text-xs font-medium transition-colors hover:bg-slate-50';
      });
      target.className = 'date-filter-btn shrink-0 bg-sky-500 text-white border-sky-500 border rounded-lg py-1.5 px-3 text-xs font-medium transition-colors';

      renderTable();
    });
  });

  let appointmentToDelete: string | null = null;

  (window as any).confirmDeleteAppointment = (id: string) => {
    appointmentToDelete = id;
    const modal = document.getElementById('delete-modal');
    if (modal) modal.classList.replace('hidden', 'flex');
  };

  (window as any).closeDeleteModal = () => {
    appointmentToDelete = null;
    const modal = document.getElementById('delete-modal');
    if (modal) modal.classList.replace('flex', 'hidden');
  };

  (window as any).executeDelete = async () => {
    if (!appointmentToDelete) return;
    const id = appointmentToDelete;
    (window as any).closeDeleteModal();
    
    const { error } = await supabase.from('appointments').delete().eq('id', id);
    
    if (error) {
      console.error('Delete error:', error);
      const errBanner = document.getElementById('delete-error');
      const errMsg = document.getElementById('delete-error-msg');
      if (errBanner && errMsg) {
        errMsg.textContent = error.message || 'Unknown error occurred.';
        errBanner.classList.replace('hidden', 'flex');
        setTimeout(() => errBanner.classList.replace('flex', 'hidden'), 5000);
      }
    } else {
      allAppointments = allAppointments.filter((a: any) => a.id !== id);
      renderTable();
    }
  };

  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') {
      (window as any).closeDeleteModal();
    }
  });

  loadAppointments();
</script>
